apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend-svc
  labels:
    service: udagram-frontend
spec:
  ports:
  - name: "8100"
    port: 8100
    targetPort: 8100
  selector:
    service: udagram-frontend